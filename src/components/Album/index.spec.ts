import * as React from 'react';
import { shallow } from 'enzyme';

import "../../setupTests";

import AlbumComponent from './index';

const fg = <AlbumComponent />

it('should render without crushing',() => {
});
